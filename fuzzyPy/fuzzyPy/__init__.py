import numpy as np
# Implementing different membership functions
def trimf(x,[a,b,c]):
    # Implements Triangular Shape Membership function
    # a: is start of triangle, b: peak, and c the end of triangle
    f=np.zeros(
